# gradient-boosted-trees-from-scratch
Histogram-based gradient boosted decision trees (regression + binary classification), implemented from scratch in NumPy — no XGBoost/LightGBM/sklearn dependency. Newton-boosted trees, shrinkage, subsampling, early stopping, and a CLI.
